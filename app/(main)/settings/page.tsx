import ToggleThemeButton from "@/app/(main)/settings/components/ToggleThemeButton";

const Page = () => {
    return (
        <div className="
                flex
                justify-center
                items-center
                w-full
            "
        >
            <ToggleThemeButton/>
        </div>
    );
};

export default Page;